

function isATreehugger(caresAboutClimateChange, Name) {
    if (caresAboutClimateChange) {
        return "Yes, " + Name + " is a treehugger, and should be proud of it."
    } else {
        return "No, " + Name + " should probably do some research."
    }
}
